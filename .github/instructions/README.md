# Instructions folder

Place prescriptive, file-type or domain-specific working rules here. Files should include `applyTo` frontmatter so the loader can auto-apply them to matching source files.

Example frontmatter:

---
applyTo: src/**/*.cs
---

One-line rule bullets follow.
